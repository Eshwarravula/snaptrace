import { createClient } from "@supabase/supabase-js";
import { certificateTypes, RequestPayload } from "./paperfix";
const url=process.env.NEXT_PUBLIC_SUPABASE_URL||"";const key=process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||"";
export const supabase=url&&key?createClient(url,key):null;
export function adminOk(req:Request){const pin=process.env.NEXT_PUBLIC_ADMIN_PIN||(process.env.NODE_ENV==="development"?"1234":"");return Boolean(pin&&req.headers.get("x-admin-pin")===pin);}
export function token(){const d=new Date(),m=String(d.getMonth()+1).padStart(2,"0"),day=String(d.getDate()).padStart(2,"0"),r=Math.floor(1000+Math.random()*9000);return`PF-${d.getFullYear()}${m}${day}-${r}`;}
export function validate(p:RequestPayload){if(!p.full_name?.trim())return"Full name is required.";if(!p.phone?.trim())return"WhatsApp number is required.";if(!/^(\+91)?[6-9]\d{9}$/.test(p.phone.trim().replace(/\s/g,"")))return"Enter a valid Indian WhatsApp number.";if(!certificateTypes.includes(p.certificate_type))return"Certificate type is required.";if(!p.district?.trim())return"District is required.";if(!p.mandal?.trim())return"Mandal is required.";if(!["New application","Already applied"].includes(p.request_type))return"Please select new application or already applied.";if(p.request_type==="Already applied"&&!p.application_number?.trim())return"Application number is required for already applied requests.";if(!["Normal","Urgent"].includes(p.urgency))return"Urgency is required.";if(!["Yes","No","Not sure"].includes(p.documents_ready))return"Please select if documents are ready.";if(!p.consent_accepted)return"Please accept the disclaimer to continue.";return"";}
